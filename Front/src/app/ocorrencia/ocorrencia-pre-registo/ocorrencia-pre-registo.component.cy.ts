import { OcorrenciaPreRegistoComponent } from './ocorrencia-pre-registo.component'

describe('OcorrenciaPreRegistoComponent', () => {
  it('should mount', () => {
    cy.mount(OcorrenciaPreRegistoComponent)
  })
})